-- Definición de servicios indispensables para evitar crasheos en celular
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local RunService = game:GetService("RunService")

-- TABLA DE LA BLACKLIST: ¡Ahora puedes poner solo las iniciales o etiquetas de clan!
local Blacklist = {
    "KTA",             -- Detectará a KTA_RAFA, KTA_LOBO, etc.
    "Toxic",           -- Detectará a cualquier jugador con la palabra "Toxic"
    "MH_CrisXIsabella"    -- También puedes seguir usando nombres completos
}

-- Variables globales de control para los bucles
_G.fastHitActive = false
local following = false
local followTargetName = nil

-- FUNCIÓN MODIFICADA: Ahora busca si el nombre CONTIENE la palabra clave
local function estaEnBlacklist(player)
    local playerNameLower = player.Name:lower()
    local playerDisplayLower = player.DisplayName:lower()
    
    for _, name in pairs(Blacklist) do
        local keyword = name:lower()
        -- Usa string.find para detectar si el fragmento está dentro del nombre
        if string.find(playerNameLower, keyword) or string.find(playerDisplayLower, keyword) then
            return true
        end
    end
    return false
end

-- Función de teletransporte constante (Bucle por detrás del juego)
local function iniciarSeguimiento(targetPlayer)
    if not targetPlayer then return end
    if following then return end
    
    followTargetName = targetPlayer.Name
    following = true
    _G.fastHitActive = true
    
    -- Bucle súper veloz de teletransporte pegado al enemigo (detrás de su espalda)
    task.spawn(function()
        while following and targetPlayer and targetPlayer.Character and targetPlayer.Character:FindFirstChild("HumanoidRootPart") do
            local enemyHRP = targetPlayer.Character.HumanoidRootPart
            local myChar = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
            local myHRP = myChar:WaitForChild("HumanoidRootPart", 3)
            
            if myHRP then
                myHRP.CFrame = enemyHRP.CFrame * CFrame.new(0, 0, 2)
            end
            RunService.Heartbeat:Wait()
        end
        following = false
        followTargetName = nil
    end)
    
    -- Hilo 1 del Auto Punch
    task.spawn(function()
        while _G.fastHitActive do
            local punch = LocalPlayer.Backpack:FindFirstChild("Punch")
            if punch then
                punch.Parent = LocalPlayer.Character
                if punch:FindFirstChild("attackTime") then
                    punch.attackTime.Value = 0
                end
            end
            task.wait(0.1)
        end
    end)
    
    -- Hilo 2 del Auto Punch
    task.spawn(function()
        while _G.fastHitActive do
            local punch = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Punch")
            if punch then
                punch:Activate()
            end
            task.wait(0.1)
        end
    end)
end

-- Escáner de seguridad para procesar ingresos de usuarios
local function procesarFiltroJugador(player)
    if player == LocalPlayer then return end
    
    if estaEnBlacklist(player) then
        game:GetService("StarterGui"):SetCore("ChatMakeSystemMessage", {
            Text = "[RADAR INTELIGENTE] ¡Enemigo detectado por coincidencia ("..player.DisplayName..")! Iniciando ataque...",
            Color = Color3.fromRGB(255, 0, 0),
            Font = Enum.Font.SourceSansBold
        })
        iniciarSeguimiento(player)
    end
end

-- Conexión de los eventos de entrada al servidor
for _, p in pairs(Players:GetPlayers()) do procesarFiltroJugador(p) end
Players.PlayerAdded:Connect(procesarFiltroJugador)

-- Manejo en caso de que el jugador cazado muera o se salga de la partida
Players.PlayerRemoving:Connect(function(player)
    if followTargetName == player.Name then
        following = false
        _G.fastHitActive = false
        followTargetName = nil
        local punch = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Punch")
        if punch then punch.Parent = LocalPlayer.Backpack end
    end
end)

-- Mensaje de confirmación visual en el chat
game:GetService("StarterGui"):SetCore("ChatMakeSystemMessage", {
    Text = "[SISTEMA] Radar por coincidencia de iniciales activado en segundo plano.",
    Color = Color3.fromRGB(0, 255, 0),
    Font = Enum.Font.SourceSansBold
})

